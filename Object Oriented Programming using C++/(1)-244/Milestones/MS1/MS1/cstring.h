/*/////////////////////////////////////////////////////////////////////////
						 Milestone - 1
Full Name  : Ekansh
Email      : ekansh@myseneca.ca
Student ID#: 167454214
Section    : OOP 244 -NAA

Authenticity Declaration:
I have done all the coding by myself and only copied the code that my
professor provided to complete my workshops and assignments.
/////////////////////////////////////////////////////////////////////////*/

/*

#ifndef SDDS_CSTRING_H_
#define SDDS_CSTRING_H_

#include <iostream>
#include <stdio.h>

namespace sdds {

	// 1.Copies the source character string into the destination
	void strCpy(char* des, const char* src);

	// 2. Copies the source character string into the destination upto len length 
	void strnCpy(char* des, const char* src, int len);

	// 3. Compares two C-strings
	int strCmp(const char* s1, const char* s2);

	// 3. Compares two C-strings upto len length 
	int strnCmp(const char* s1, const char* s2, int len);

	// 5 . returns the lenght of the C-string in characters
	int strLen(const char* s);

	// 6. returns the address of first occurance of "str2" in "str1"
		 // returns nullptr if no match is found
	const char* strStr(const char* str1, const char* str2);

	// 7. Concantinates "src" C-string to the end of "des"
	void strCat(char* des, const char* src);
}
#endif // !SDDS_CSTRING_H_




*/